profiles {
    standard {
        process {
            executor = 'local'
            cpus = 1
            memory = '2 GB'
        }
    }

    docker {
        docker.enabled = true
    }
}

// minimal params
params {
    samples = "samples.csv"
    outputDir = "results"
}
